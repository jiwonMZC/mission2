version: 0.2
phases:
  pre_build:
    commands:
      - aws ec2 describe-network-interfaces
      - echo "pre build"
  build:
    commands:
      - echo "building"
  post_build:
    commands:
      - echo "post build"
      - cp -r $CODEBUILD_SRC_DIR/* $CODEBUILD_MY_EFS
